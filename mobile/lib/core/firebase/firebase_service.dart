class FirebaseService {
  static Future<void> initialize() async {
    // Firebase initialization will be connected here.
    // Kept isolated to allow future configuration without changing app layers.
  }
}
